@using Microsoft.AspNetCore.Mvc.Localization
@model AddRoleVM
@inject IViewLocalizer localizer
<form method="post" asp-action="AddUserRole" asp-controller="AppRoles">
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">@localizer["Rol Oluştur"]</h3>
        </div>

        <div class="card-body" style="display: block;">

            <div class="form-group">
                <input type="text" asp-for="Name" class="form-control" placeholder="@localizer["Adı"]">
                <span asp-validation-for="Name"></span>
            </div>
@*            <div class="form-group">
                <label>@localizer["İzinler"]</label>
                <select asp-for="Policies" class="form-control">
                    @foreach (var policy in Model.Policies)
                    {
                    <option onselect="@policy.IsSelected=true">@policy.Policy</option>
                    }
                </select>
            </div>*@

        </div>


        <div class="card-footer">

            <a asp-action="GetAllUserRoles" asp-controller="AppRoles" class="btn btn-secondary col-2">@localizer["Geri Dön"]</a>
            <input type="submit" value="@localizer["Rol Oluştur"]" class="btn btn-success col-2 float-right">
        </div>
    </div>
</form>