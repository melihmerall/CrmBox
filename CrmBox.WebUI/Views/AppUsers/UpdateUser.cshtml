@using Microsoft.AspNetCore.Mvc.Localization
@model UpdateUserVM
@inject IViewLocalizer localizer
@*TODO: Bu sayfa Update user eklendikten sonra düzenlenecek *@
<form method="post" asp-action="UpdateUser" asp-controller="AppUsers">
    @Html.HiddenFor(x => x.Id)
    <div class="card card-primary m-3" />
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">@localizer["Rol Güncelle"]</h3>
        </div>
        <div class="card-body" style="display: block;">

            <div class="form-group">
                <label asp-for="UserName">@localizer["UserName"]</label>
                <input type="text" asp-for="UserName" class="form-control" placeholder="@Model.UserName">
            </div>
        </div>
        <div class="card-body" style="display: block;">

            <div class="form-group">
                <label asp-for="FirstName">@localizer["FirstName"]</label>
                <input type="text" asp-for="FirstName" class="form-control" placeholder="@Model.FirstName">
            </div>
        </div>
        <div class="card-body" style="display: block;">

            <div class="form-group">
                <label asp-for="LastName">@localizer["LastName"]</label>
                <input type="text" asp-for="LastName" class="form-control" placeholder="@Model.LastName">
            </div>
        </div>
@*
        <div class="card-body" style="display: block;">

            <div class="form-group">
                <label asp-for="Roles">@localizer["Role"]</label>
                <input type="text" asp-for="Roles" class="form-control" placeholder="@Model.Roles">
            </div>
        </div>*@
@*        <div class="form-group">
            <label>@localizer["İzinler"]</label>
            <select asp-for="Roles" class="form-control">
            @foreach (var role in Model.Roles)
            {
            <option onselect="@role.IsSelected=true">@role.name</option>
            }
            </select>
            </div>*@


        <div class="card-footer">
            <a asp-action="GetAllUsers" asp-controller="AppUsers" class="btn btn-secondary col-2">@localizer["Geri Dön"]</a>
            <button type="submit" class="btn btn-success col-2 float-right">@localizer["Kullanıcı Güncelle"] </button>
        </div>
    </div>
</form>